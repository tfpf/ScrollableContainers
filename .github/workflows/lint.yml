name: lint
on: [push, workflow_dispatch]

jobs:
  lint:
    name: lint
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt install hatch
      - run: hatch fmt --check
